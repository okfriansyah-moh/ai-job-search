# JS Remotely source contract

- Canonical public listing page: `GET https://javascript.jobs/remote?keyword=<query>`
- Per-card anchors: job URL beginning `/job/`, title `tw-text-lg`, company `tw-card-title`, employment type badge.
- The lightweight HTML parser keeps only complete title, company, and canonical job URL records.
