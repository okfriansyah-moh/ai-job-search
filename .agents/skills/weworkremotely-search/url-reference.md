# We Work Remotely source contract

- Feed: `GET https://weworkremotely.com/remote-jobs.rss`
- Per-item fields: `title` (`Company: Role`), `link`, `region`, `country`, `description`, `pubDate`, and `type`.
- RSS description HTML is reduced to readable text; no links from inside a posting are followed.
